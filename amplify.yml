version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --prefix app
    build:
      commands:
        - npm run build --prefix app
  artifacts:
    baseDirectory: app/dist
    files:
      - "**/*"
  cache:
    paths:
      - app/node_modules/**/*
